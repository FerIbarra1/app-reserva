{
    "buildCommand": "cd app-reserva && yarn install && ./node_modules/vite/bin/vite.js build",
    "outputDirectory": "app-reserva/dist",
    "framework": "vite",
    "rewrites": [
        { 
            "source": "/api/(.*)",
            "destination": "/api/index.js"
        }
    ]
}